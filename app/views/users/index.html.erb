<%= form_for(@book, url: books_path) do |f| %>
          <% if @book.errors.any? %>
            <div id="error_explanation">
              <p><%= @book.errors.count %>errors prohibited this obj from being saved:</p>
              <ul>
              <% @book.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
              </ul>
            </div>
          <% end %>
<% end %>
<div class="container">
  <div class="row">
    <div class="col-xs-3">
        <%= render 'users/list', user: @user %>

        <br>
        <%= render 'books/list', book: @book %>

    </div>

    <div class="col-xs-9">
      <h1>Users</h1>
      <table class="table">
        <thead>
          <tr>
            <th>image</th>
            <th>name</th>
            <th></th>
          </tr>
        </thead>
        <% if @users.present? %>
        <% @users.each do |user| %>
          <tbody>
            <tr>
              <th><%= attachment_image_tag user, :profile_image, :fill, 50, 50, format: 'jpeg', fallback: "no_image.jpg", size:'50x50' %></th>
              <td><%= user.name %></td>
              <td><%=link_to 'Show', user_path(user) %></td>
            </tr>
          </tbody>
        <% end %>
        <% end %>
      </table>
    </div>
  </div>
</div>
