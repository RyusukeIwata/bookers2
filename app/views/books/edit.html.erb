<div class="col-md-6 col-md-ofset-3">
	<%= form_for(@book) do |f| %>
		<% if @book.errors.any? %>
			<h3><%= @book.errors.count %>error prohibited this book from being saved:</h3>
			<ul>
				<% @book.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		<% end %>
		<h1>Editing Book</h1>
		<p>Title</p>
		<%= f.text_field :title %>
		<p>Opinion</p>
		<%= f.text_area :body %>
		<br>
		<%= f.submit 'Update Book' %>
	<% end %>
	<%=link_to 'Show', book_path(@book) %><span> |</span>
	<%=link_to 'Back', books_path %>
</div>
